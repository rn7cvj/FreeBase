import 'package:flutter/material.dart';

class ProjectOverviewPortrait extends StatelessWidget {
  const ProjectOverviewPortrait({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
